spring:
  profiles:
    include: api
  jpa:
    open-in-view: false
    show-sql: true
    properties:
      hibernate.format_sql: true
  output:
    ansi:
      enabled: always

jwt:
  secret: secret