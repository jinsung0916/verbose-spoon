spring:
  profiles:
    include: api
  jpa:
    open-in-view: false
    show-sql: true
    properties:
      hibernate.format_sql: true
  output:
    ansi:
      enabled: always
  mail:
    host: HOST_NOT_DEFINED
    username: USERNAME_NOT_DEFINED

jwt:
  secret: secret